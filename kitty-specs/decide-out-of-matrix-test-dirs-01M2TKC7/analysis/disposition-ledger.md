# Disposition Ledger

**Mission**: decide-out-of-matrix-test-dirs-01M2TKC7 · **Issue**: #4374 · **WP**: WP01 (Disposition analysis
& ledger) · **Authoritative for**: WP02 (lift-invariant authoring), WP03 (promote/measure), WP04 (registry
edit + permanent guards). **This ledger is the single decision source -- WP02-WP04 execute it, they do not
re-decide.**

Governed by `contracts/disposition-decision-procedure.md` (v2) and `contracts/acceptance-checks.md` (v3).
Scope: all 128 targeted dirs (60 population-(a) secondary `tests/specify_cli/**` trees + 68 population-(b)
no-per-PR-lane trees). Excluded (untouched, not in this ledger): `tests/specify_cli/cli/commands` (#4715) and
the 5 named-home entries (`tests`, `tests/docs`, `tests/e2e`, `tests/architectural`, `tests/ui`).

## Disposition mix

| Disposition | Count | Meaning |
|---|---|---|
| 1 - promote-per-pr | 9 | Existing/new module row, WP03 adds `test_dirs` + measures + resizes shard_count. Capped to the Phase-S security-first set: `merge`, `status` (+ bundled `write_side`), `dashboard` (all existing rows) and `auth` (new row via census-gated scrub). |
| 1x - deferred-promotion | 81 | A decision, not a snapshot: unique behavioral coverage exists, but either (a) an existing owning row was found and is NOT in the capped set, or (b) no owning row exists (rootless) and minting one is out of this single-PR's cap. Every row names its owner ("existing row X" or "none -- needs new row via scrub") and a rationale; several carry an explicit #2979 marker-gap flag. Promotion/marking is warranted and deferred to a follow-up (#4374 successor), never silently absorbed. |
| 2 - lift-invariant | 13 | Unique value is one or a small, enumerable set of single-topic repo-invariant/regression-freeze checks (the #4715 pattern). WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py`; T006 (below) names the concrete set. |
| 3 - sharpen-reason | 25 | Kept out-of-matrix with a VERIFIED (not snapshot) reason: 7 already claimed by `special_tiers.integration_tests_next` (PC3, job currently unwired -- flagged), 18 with a grep-confirmed pytest marker naming their real nightly/corpus home (EG2). |

**Sum check**: 9 + 81 + 13 + 25 = 128. ✓ (script-verified against the registry's own 60+68 enumeration --
see `analysis/evidence/` for the generating census.)

## Pre-check findings (T001/T002, binding on WP02-WP04)

- **PC1 (nested-family)**: exactly two dirs are promotion-blocked as a unit: `tests/specify_cli/cli`
  (pop-a; nests the excluded `tests/specify_cli/cli/commands` + the execution_context-claimed
  `tests/specify_cli/cli/commands/agent`) and `tests/specify_cli` (pop-b; nests multiple claimed
  descendants plus the same excluded dir). Both resolve to disposition 2 (lift) below, not 1/3, since their
  own direct files are narrow, enumerable invariant/regression checks rather than broad behavioral coverage.
  No other dir in the 128 has an excluded/claimed descendant (full recursive scan recorded in
  `analysis/evidence/pc1_nested_family_analysis.txt`).
- **PC2 (ownership tie-break)**: `src/specify_cli/status/**` is a root in FOUR rows (`status`,
  `execution_context`, `core_misc`, `unit`). Per PC2, `status` wins for both `tests/specify_cli/status` (leaf
  name match) and `tests/specify_cli/write_side` (root-path match via the row's second root,
  `src/specify_cli/coordination/**`, confirmed by import census) -- never one of the three aggregates. No
  other shared-root ambiguity was found among the 128 (`status` is the only root claimed by more than one
  row that also has a targeted-dir match).
- **PC3 (special-tier)**: the 7 `tests/integration/**` dirs are already claimed by
  `special_tiers.integration_tests_next` (roots `tests/integration/**`, `tests/next/**`; job
  `integration-tests-next`, ~69min measured, currently **unwired** -- no triggering workflow invokes it,
  a pre-existing honesty gap this ledger surfaces rather than papers over). `tests/specify_cli/next` (pop-a)
  is NOT under this tier's roots (different path prefix) and is dispositioned separately against the `next`
  module row.
- **Registry-ceiling due diligence**: the registry's own description warns of "GitHub's
  20-reusable-workflows-per-caller ceiling," and `modules[]` currently holds exactly 20 rows. Verified this
  does NOT block the `auth` new-row promotion: `ci-modules.yml` (lines 293-297) fans ALL module rows out
  through ONE `uses: ./.github/workflows/module-tests.yml` reusable-workflow reference via
  `strategy.matrix` -- a 21st module row is one more matrix cell, not a new `uses:` statement.

## Capped promotion set (Phase S, security-first)

Per the contract's Cap (priti F6) and the plan's amended Phase S: this PR's disposition-1 rows are limited to
**existing rows with clean PC1** (`merge`, `status` bundled with `write_side`, `dashboard`) **plus the one
security-critical new-row-via-scrub the cap explicitly allows** (`auth`, 5 dirs). No other promotion is
proposed for execution by this mission; every other existing-row candidate (`review`, `missions`,
`post_merge`, `lanes`, `next`, `agent`, `upgrade`, `charter` x6, `cli` x2) is recorded as
**deferred-promotion**, honestly naming its owner, not silently sharpened away.

## Lift-invariant set (T006 -- feeds WP02)

Two PC1-blocked parents get bulk lift treatment; several small rootless trees get individual lift treatment.
WP02 authors one `tests/architectural/test_lifted_<x>.py` per named invariant (assertion shape: same style as
the #4715 precedent, `pytestmark = [pytest.mark.architectural]`, no fixtures beyond repo-path resolution).

### `tests/specify_cli/cli` (9 direct files -- ALL enumerated, small enough to lift completely)

| Source test | Invariant | Target always-on test | Assertion shape |
|---|---|---|---|
| `test_no_visible_feature_alias.py` | Every `--feature` CLI flag stays a HIDDEN alias; zero visible `--feature` flags exist CLI-wide (Terminology Canon). | `tests/architectural/test_lifted_cli_no_visible_feature_alias.py` | Walk the live Typer/Click command tree; assert no visible option is named `--feature`; assert every `--feature` alias, if present, is `hidden=True`. |
| `test_doctrine_cli_removed.py` | The retired `doctrine curate`/`doctrine promote` commands stay unregistered; the `doctrine` group itself stays registered. | `tests/architectural/test_lifted_cli_doctrine_retirement.py` | Introspect the CLI app's command registry; assert absence of the retired subcommands, presence of the group. |
| `test_bytecode_heal_startup.py` | A corrupt `.pyc` (migrations or discovery-only) does not crash CLI startup; the planner/upgrade seams heal through it. | `tests/architectural/test_lifted_cli_bytecode_heal_startup.py` | Write a corrupt `.pyc` fixture; invoke the CLI entrypoint in-process; assert exit 0 / no traceback. |
| `test_accept_coord_surface_anchor.py` | The `accept` coord-stamp leg does not round-trip through git; a deleted coordination branch propagates correctly. | `tests/architectural/test_lifted_cli_accept_coord_surface_anchor.py` | Same structural assertion, no git-network dependency. |
| `test_accept_birth_cutover.py` | Post-squash-merge `accept` lands a cut-over corpus; the accept stamp is idempotent and byte-identical on rerun; stamping fails closed when `mission_id` is absent. | `tests/architectural/test_lifted_cli_accept_birth_cutover.py` | Fixture-driven idempotency + fail-closed assertions (no full merge simulation needed). |
| `test_decision_command_shape_consistency.py` | The `agent decision` subgroup exposes only canonical visible subcommands; help output lists them; no non-canonical decision-command shape appears anywhere in repo text. | `tests/architectural/test_lifted_cli_decision_command_shape.py` | Introspect the command tree + `git grep` repo text for the banned shape. |
| `test_terminal_control_sanitization.py` | Human-facing strings (incl. nested Rich renderables, external-interview hints, glossary/git error output) are sanitized before markup; machine JSON output stays plain and round-trips; str/segment rendering share one policy. | `tests/architectural/test_lifted_cli_terminal_control_sanitization.py` | Feed a control-char payload through the shared sanitization seam; assert stripped output + JSON round-trip. |
| `test_doctrine_charter_cr02_compat.py` | The `doctrine` group's hidden alias warns; the `charter` group's canonical routes still resolve (CR02 compat). | `tests/architectural/test_lifted_cli_doctrine_charter_cr02_compat.py` | Invoke both paths in-process; assert warning emission + canonical routing. |
| `test_helpers.py` | `get_project_root_or_exit` succeeds in a worktree and fails with the correct message/exit-code/JSON envelope when git resolution fails. | `tests/architectural/test_lifted_cli_project_root_resolution.py` | Fixture worktree + a git-unavailable fixture; assert message/exit/JSON shape. |

### `tests/specify_cli` (root, ~70 direct files -- TOO LARGE to enumerate exhaustively here; representative sample only, WP02 must triage/batch the remainder by family)

Naming convention alone shows the tree is dominated by single-topic regression/guard/contract checks
(`test_*_regression`, `test_*_guard`, `test_*_contract`, `test_*_migration`, `test_*_gate*`) spanning many
unrelated src areas. Representative sample for WP02 to start from (NOT the full set):

| Source test | Invariant (as named) | Target always-on test |
|---|---|---|
| `test_no_manual_ffmerge.py` | No manual fast-forward merge path exists in the codebase. | `test_lifted_root_no_manual_ffmerge.py` |
| `test_no_checklist_surface.py` | No ad-hoc checklist surface exists outside the canonical one. | `test_lifted_root_no_checklist_surface.py` |
| `test_gitignore_contract.py` | The `.gitignore` contract stays intact. | `test_lifted_root_gitignore_contract.py` |
| `test_command_template_cleanliness.py` | Command templates stay clean (no drift/leftover placeholders). | `test_lifted_root_command_template_cleanliness.py` |
| `test_state_contract.py` | The state contract stays intact across the repo. | `test_lifted_root_state_contract.py` |
| `test_terminal_state_gate_integrity.py` | The terminal-state gate's integrity invariant holds. | `test_lifted_root_terminal_state_gate_integrity.py` |
| `test_validate_mission_paths_single_caller.py` | Mission-path validation has exactly one caller (no duplicate validators). | `test_lifted_root_validate_mission_paths_single_caller.py` |
| `test_meta_fail_closed_full_census_contract.py` | `meta.json` fail-closed behavior is census-complete (every read path covered). | `test_lifted_root_meta_fail_closed_census.py` |

**WP02 guidance**: batch by family (charter-lifecycle, meta-fail-closed, mission-metadata, doctor,
acceptance-gate, ...) rather than one-by-one; not every one of the ~70 files is necessarily a clean single
invariant on closer reading -- WP02 re-verifies each candidate before lifting, this ledger only identifies
the tree's disposition (2) and a representative starting sample.

### Small rootless single/few-file lifts (population-b)

| Source test | Invariant | Target always-on test |
|---|---|---|
| `tests/lint/test_canonical_producers.py` | Canonical-producer lint invariant. | `test_lifted_lint.py` |
| `tests/evidence/test_project_sync_consent_manifest.py` | Project-sync consent-manifest invariant. | `test_lifted_evidence.py` |
| `tests/proof/test_event_schemas.py` | Event-schema proof invariant. | `test_lifted_proof.py` |
| `tests/state/test_doctor_spec_kitty_home.py` | `spec-kitty` home doctor-state invariant. | `test_lifted_state.py` |
| `tests/reviews/test_review_gate_activation.py` | Review-gate activation invariant. | `test_lifted_reviews.py` |
| `tests/mission_metadata/test_mission_identity.py` | Mission-identity (ULID/mid8) invariant. | `test_lifted_mission_metadata.py` |
| `tests/task_utils/test_set_scalar_retired.py` | `set_scalar` retirement invariant. | `test_lifted_task_utils.py` |
| `tests/packaging/test_windows_no_keyring.py` | Windows no-keyring packaging invariant. **#2979 flag**: filename implies Windows-specific behavior but NO `windows_ci` marker was found on this tree (`analysis/evidence/pop_b_marker_grep.txt`) -- a real, concrete marker-invisibility gap. WP02 must confirm the assertion is genuinely platform-independent (safe to lift as always-on) before lifting, or route it to get a real `windows_ci` marker instead. | `test_lifted_packaging_windows_keyring.py` |
| `tests/specify_cli/mission_v1/test_import_hygiene.py` | Import-hygiene structural invariant. | `test_lifted_mission_v1_import_hygiene.py` |
| `tests/specify_cli/drg_writers/test_registry_completeness.py` | DRG-writer registry-completeness invariant. | `test_lifted_drg_writers_registry_completeness.py` |
| `tests/specify_cli/docs/test_trail_model_doc.py` | Trail-model doc-freshness invariant. | `test_lifted_specify_cli_docs_trail_model.py` |
| `tests/specify_cli/docs/test_host_surface_inventory.py` | Host-surface-inventory doc-freshness invariant. | `test_lifted_specify_cli_docs_host_surface_inventory.py` |
| `tests/specify_cli/docs/test_readme_governance.py` | README-governance freshness invariant. | `test_lifted_specify_cli_docs_readme_governance.py` |

## Flagged cross-cutting findings (not individually dispositioned, for follow-up triage)

- **Similarly-named tree clusters, none consolidated in this WP** (each dir IS individually dispositioned
  above; this is a naming-hygiene flag, not a blocker): `retrospect`/`retrospective` x3 (pop-a `retrospect`
  1 file, pop-a `retrospective` 8 files, pop-b `retrospective` 22 files/marker-verified); `migration`/
  `migrate` x3 (pop-a `migration` 25 files, pop-b `migration` 12 files/marker-verified, pop-b `migrate` 1
  file); `tracker` x2 (pop-a 1 file, pop-b 27 files/marker-verified); `dossier` x2 (pop-a 1 file, pop-b 15
  files); `git`/`git_ops` (pop-a `git` 2 files, pop-b `git` 5 files/marker-verified, pop-b `git_ops` 18
  files); `compat`/`context`/`invocation`/`tasks` each have a pop-a AND a distinct pop-b tree with the same
  leaf name testing DIFFERENT src surfaces (confirmed by import census, not accidental duplicates).
- **#2979 marker-gap findings** (T004): of the 68 population-(b) dirs, 45 raw-grep as carrying NO
  `{corpus, windows_ci, performance, e2e, stress}` marker; 8 of those (auth family: `concurrency`,
  `integration`, `stress`) are covered anyway by the auth new-row promotion and 5 (integration family:
  `core`, `git`, `migration`, `retrospective`, `sparse_checkout`) are covered by the PC3 special-tier claim,
  so marker absence is immaterial there. Of the remaining 37 non-auth/non-integration dirs, 8 small
  single-file trees were reclassified as lift-invariant (T006) and **29 stay flagged**: NO marker despite the
  pre-mission reason implying marker-reachability, dispositioned deferred-promotion with an explicit gap note.
  One, `tests/packaging` (filename `test_windows_no_keyring.py`), is a concrete, named case where the marker
  is plausibly warranted but absent. See `analysis/evidence/pop_b_marker_grep.txt` for the full per-tree
  result.
- **#4426 closure** (SC-008): both #4426 dirs are decided in this ledger -- `tests/performance` (pop-b,
  marker-verified sharpen) and `tests/specify_cli/live_work` (pop-a, deferred-promotion, rootless).

## Full ledger (128 rows)

| dir | population | disposition | owner/mirror | rationale | evidence-pointer | phase |
|---|---|---|---|---|---|---|
| `tests/auth` | b | 1 - promote-per-pr | auth (NEW row via scrub) | Security-critical (per spec Phase S explicit allowance: the ONE new-row-via-scrub this mission's cap permits). Real src root confirmed: `src/specify_cli/auth/**` (device_flow, http, secure_storage, flows, loopback, websocket, token_manager, manager, session, session_hot_path, transport, config, verdict, errors) -- no existing scrub group owns it (checked `tests/release/ci_retirement_scrub.json`). Minting a row requires (1) a census-authorized scrub group, (2) a verbatim-mirrored registry row -- both two-file edits, heavier than an existing-row add. Verified NOT blocked by the registry's 20-reusable-workflow-ceiling comment: all `modules[]` rows share ONE `uses: ./.github/workflows/module-tests.yml` reusable-workflow reference via `strategy.matrix` (ci-modules.yml:293-297) -- a 21st module row is one more matrix cell, not a new `uses:` reference, so the ceiling is not a blocker. Disposition: PROMOTE (new-row-via-scrub) -- WP03 mints the scrub group + registry row + measured capture; this WP only certifies the root and the non-blocker. | n/a (WP03 mints scrub group + row + measures) | S |
| `tests/auth/concurrency` | b | 1 - promote-per-pr | auth (NEW row via scrub) | Security-critical (per spec Phase S explicit allowance: the ONE new-row-via-scrub this mission's cap permits). Real src root confirmed: `src/specify_cli/auth/**` (device_flow, http, secure_storage, flows, loopback, websocket, token_manager, manager, session, session_hot_path, transport, config, verdict, errors) -- no existing scrub group owns it (checked `tests/release/ci_retirement_scrub.json`). Minting a row requires (1) a census-authorized scrub group, (2) a verbatim-mirrored registry row -- both two-file edits, heavier than an existing-row add. Verified NOT blocked by the registry's 20-reusable-workflow-ceiling comment: all `modules[]` rows share ONE `uses: ./.github/workflows/module-tests.yml` reusable-workflow reference via `strategy.matrix` (ci-modules.yml:293-297) -- a 21st module row is one more matrix cell, not a new `uses:` reference, so the ceiling is not a blocker. Disposition: PROMOTE (new-row-via-scrub) -- WP03 mints the scrub group + registry row + measured capture; this WP only certifies the root and the non-blocker. | n/a (WP03 mints scrub group + row + measures) | S |
| `tests/auth/integration` | b | 1 - promote-per-pr | auth (NEW row via scrub) | Security-critical (per spec Phase S explicit allowance: the ONE new-row-via-scrub this mission's cap permits). Real src root confirmed: `src/specify_cli/auth/**` (device_flow, http, secure_storage, flows, loopback, websocket, token_manager, manager, session, session_hot_path, transport, config, verdict, errors) -- no existing scrub group owns it (checked `tests/release/ci_retirement_scrub.json`). Minting a row requires (1) a census-authorized scrub group, (2) a verbatim-mirrored registry row -- both two-file edits, heavier than an existing-row add. Verified NOT blocked by the registry's 20-reusable-workflow-ceiling comment: all `modules[]` rows share ONE `uses: ./.github/workflows/module-tests.yml` reusable-workflow reference via `strategy.matrix` (ci-modules.yml:293-297) -- a 21st module row is one more matrix cell, not a new `uses:` reference, so the ceiling is not a blocker. Disposition: PROMOTE (new-row-via-scrub) -- WP03 mints the scrub group + registry row + measured capture; this WP only certifies the root and the non-blocker. | n/a (WP03 mints scrub group + row + measures) | S |
| `tests/auth/secure_storage` | b | 1 - promote-per-pr | auth (NEW row via scrub) | Security-critical (per spec Phase S explicit allowance: the ONE new-row-via-scrub this mission's cap permits). Real src root confirmed: `src/specify_cli/auth/**` (device_flow, http, secure_storage, flows, loopback, websocket, token_manager, manager, session, session_hot_path, transport, config, verdict, errors) -- no existing scrub group owns it (checked `tests/release/ci_retirement_scrub.json`). Minting a row requires (1) a census-authorized scrub group, (2) a verbatim-mirrored registry row -- both two-file edits, heavier than an existing-row add. Verified NOT blocked by the registry's 20-reusable-workflow-ceiling comment: all `modules[]` rows share ONE `uses: ./.github/workflows/module-tests.yml` reusable-workflow reference via `strategy.matrix` (ci-modules.yml:293-297) -- a 21st module row is one more matrix cell, not a new `uses:` reference, so the ceiling is not a blocker. Disposition: PROMOTE (new-row-via-scrub) -- WP03 mints the scrub group + registry row + measured capture; this WP only certifies the root and the non-blocker. | n/a (WP03 mints scrub group + row + measures) | S |
| `tests/auth/stress` | b | 1 - promote-per-pr | auth (NEW row via scrub) | Security-critical (per spec Phase S explicit allowance: the ONE new-row-via-scrub this mission's cap permits). Real src root confirmed: `src/specify_cli/auth/**` (device_flow, http, secure_storage, flows, loopback, websocket, token_manager, manager, session, session_hot_path, transport, config, verdict, errors) -- no existing scrub group owns it (checked `tests/release/ci_retirement_scrub.json`). Minting a row requires (1) a census-authorized scrub group, (2) a verbatim-mirrored registry row -- both two-file edits, heavier than an existing-row add. Verified NOT blocked by the registry's 20-reusable-workflow-ceiling comment: all `modules[]` rows share ONE `uses: ./.github/workflows/module-tests.yml` reusable-workflow reference via `strategy.matrix` (ci-modules.yml:293-297) -- a 21st module row is one more matrix cell, not a new `uses:` reference, so the ceiling is not a blocker. Disposition: PROMOTE (new-row-via-scrub) -- WP03 mints the scrub group + registry row + measured capture; this WP only certifies the root and the non-blocker. | n/a (WP03 mints scrub group + row + measures) | S |
| `tests/specify_cli/dashboard` | a | 1 - promote-per-pr | dashboard (existing row) | Security-first (dashboard-adjacent) Phase-S capped promotion. Owning row **dashboard** (cov_targets `specify_cli.dashboard`) -- import census: 29x `specify_cli.dashboard.handlers`, 8x `.handlers.lint`, 5x `.handlers.glossary`, 4x `.handlers.router`. PC1 clean. Note: dashboard already has an EXPLICIT dual `test_dirs` (`tests/dashboard`, `tests/test_dashboard`, the #4369 precedent this whole mission generalizes); this promotion adds a THIRD tree to the same row -- WP03 must re-measure the combined module, not just the new tree, to keep skew accurate. | n/a (WP03 measures) | S |
| `tests/specify_cli/merge` | a | 1 - promote-per-pr | merge (existing row) | Security-first Phase-S capped promotion. Owning row **merge** (cov_targets `specify_cli.merge`; roots `src/specify_cli/merge/**`) confirmed by import census (23x `specify_cli.merge.baseline`, 5x `specify_cli.merge`). PC1 clean (no excluded/claimed descendant). WP03 sets `merge.test_dirs: [tests/merge, tests/specify_cli/merge]` -- the explicit list REPLACES the `tests/merge` fallback claim (`_effective_test_dir_claims`), so it MUST include `tests/merge` or the guard reds; then runs `capture_shard_timings.py --module merge --write`, resizes shard_count. | n/a (WP03 measures) | S |
| `tests/specify_cli/status` | a | 1 - promote-per-pr | status (existing row) | Security-first Phase-S capped promotion. Owning row **status** is the PC2 leaf-specific winner over 3 aggregate rows that also claim `src/specify_cli/status/**` as a root (execution_context, core_misc, unit) -- status's cov_target `specify_cli.status` names the leaf, the others are aggregates that merely carry the broad root (PC2 tie-break). Import census confirms: 29x `specify_cli.status.models`, 16x `specify_cli.status`, 14x `.store`, 11x `.reducer`, 9x `.emit`. PC1 clean. WP03 sets `status.test_dirs: [tests/status, tests/specify_cli/status, tests/specify_cli/write_side]` -- the explicit list REPLACES the `tests/status` fallback claim, so it MUST include `tests/status` or the guard reds; then measures, resizes shard_count. | n/a (WP03 measures) | S |
| `tests/specify_cli/write_side` | a | 1 - promote-per-pr | status (existing row, bundled) | Bundled into the same **status** promotion above (not a separate capture): top import is `specify_cli.coordination.status_transition` (3x), which matches status row's SECOND root `src/specify_cli/coordination/**` and cov_target `specify_cli.coordination` directly -- this is not a name-leaf match but a real root-path match, so it is legitimately the same owning row, not a rootless tree. Folding it into the same WP03 status capture costs one extra `test_dirs` entry and zero extra captures (same module, same run). | n/a (WP03 measures, bundled with status) | S |
| `tests/specify_cli/cli` | a | 2 - lift-invariant | tests/architectural/ (9 lifts, see T006) | PC1 PROMOTION-BLOCKED (explicitly, per plan.md/contract v2): recursive descendants include the excluded `tests/specify_cli/cli/commands` AND the execution_context-claimed `tests/specify_cli/cli/commands/agent` -- illegal to claim as a `test_dirs` parent (claims are subtree-recursive). Its 9 DIRECT files are single-topic regression/structural-invariant checks (not broad behavioral coverage): see T006. Disposition 2 (lift), same pattern as the sibling #4715 fix for `tests/specify_cli/cli/commands`. Accepted named gap: after WP02 extracts the invariants, the residual behavioral files in this large tree remain `make test-full`-only -- lifting covers the drift-prone invariants, NOT the full behavioral surface; WP02 must not over-claim otherwise. | n/a (lift needs no measurement) | S |
| `tests/specify_cli/agent_utils` | a | 1x - deferred-promotion | agent (existing row) | Existing owning row **agent** confirmed by import census (2x `specify_cli.agent_utils.status`). PC1 clean. NOT in the Phase-S capped set (merge/status/dashboard/auth only) -> promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Owning row: agent. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/charter` | a | 1x - deferred-promotion | charter (existing row) | Existing owning row **charter** (cov_targets: `charter`, `specify_cli.charter_runtime`; roots: `src/charter/**`, `src/specify_cli/charter_runtime/**`) clean under PC1 (no excluded/claimed descendant). Confirmed by import census: top imports are `specify_cli.charter_runtime.*` / `charter.*`. Not in the Phase-S capped-promote set (merge/status/dashboard/auth only) -> deferred-promotion (#4374), not executed this WP. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/charter_freshness` | a | 1x - deferred-promotion | charter (existing row) | Existing owning row **charter** (cov_targets: `charter`, `specify_cli.charter_runtime`; roots: `src/charter/**`, `src/specify_cli/charter_runtime/**`) clean under PC1 (no excluded/claimed descendant). Confirmed by import census: top imports are `specify_cli.charter_runtime.*` / `charter.*`. Not in the Phase-S capped-promote set (merge/status/dashboard/auth only) -> deferred-promotion (#4374), not executed this WP. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/charter_lint` | a | 1x - deferred-promotion | charter (existing row) | Existing owning row **charter** (cov_targets: `charter`, `specify_cli.charter_runtime`; roots: `src/charter/**`, `src/specify_cli/charter_runtime/**`) clean under PC1 (no excluded/claimed descendant). Confirmed by import census: top imports are `specify_cli.charter_runtime.*` / `charter.*`. Not in the Phase-S capped-promote set (merge/status/dashboard/auth only) -> deferred-promotion (#4374), not executed this WP. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/charter_lint/checks` | a | 1x - deferred-promotion | charter (existing row) | Existing owning row **charter** (cov_targets: `charter`, `specify_cli.charter_runtime`; roots: `src/charter/**`, `src/specify_cli/charter_runtime/**`) clean under PC1 (no excluded/claimed descendant). Confirmed by import census: top imports are `specify_cli.charter_runtime.*` / `charter.*`. Not in the Phase-S capped-promote set (merge/status/dashboard/auth only) -> deferred-promotion (#4374), not executed this WP. Nested under the (also deferred) `tests/specify_cli/charter_lint`; same non-nesting caveat as upgrade/migrations above -- promote together, never parent-only. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/charter_preflight` | a | 1x - deferred-promotion | charter (existing row) | Existing owning row **charter** (cov_targets: `charter`, `specify_cli.charter_runtime`; roots: `src/charter/**`, `src/specify_cli/charter_runtime/**`) clean under PC1 (no excluded/claimed descendant). Confirmed by import census: top imports are `specify_cli.charter_runtime.*` / `charter.*`. Not in the Phase-S capped-promote set (merge/status/dashboard/auth only) -> deferred-promotion (#4374), not executed this WP. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/charter_runtime` | a | 1x - deferred-promotion | charter (existing row) | Existing owning row **charter** (cov_targets: `charter`, `specify_cli.charter_runtime`; roots: `src/charter/**`, `src/specify_cli/charter_runtime/**`) clean under PC1 (no excluded/claimed descendant). Confirmed by import census: top imports are `specify_cli.charter_runtime.*` / `charter.*`. Not in the Phase-S capped-promote set (merge/status/dashboard/auth only) -> deferred-promotion (#4374), not executed this WP. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/cli/commands/charter` | a | 1x - deferred-promotion | cli (existing row) | Leaf is itself PC1-clean (does not nest the excluded `tests/specify_cli/cli/commands` dir or the execution_context-claimed `commands/agent` sibling -- those are separate registry entries). Owning row **cli** (cov_targets: `specify_cli.cli`; roots `src/specify_cli/cli/**`) by import census (21x `specify_cli.cli.commands.charter`, 14x `charter_app`). A real EG1 coverage-diff capture (`pytest --cov=src/specify_cli/cli/commands/charter --cov-branch`, over this tree AND the in-matrix `tests/cli` primary mirror) was STARTED but exceeded a reasonable WP01 analysis budget (aborted after >8 minutes on a 29-test set, likely heavy CliRunner/project-init fixture overhead the same class the registry's own `upgrade`/`agent` shard-count comments describe) and was killed rather than left to block this WP -- no fabricated verdict is recorded. Lighter-weight census evidence (`grep -rl` in `tests/cli/`) DOES show 9 files / 60 test functions in `tests/cli` (incl. `tests/cli/commands/test_charter_*.py`) directly exercising `specify_cli.cli.commands.charter` -- a real, non-trivial but UNPROVEN overlap. Per the practical-evidence-strategy guidance ("cap mirror-equivalence claims ... when in doubt use deferred-promotion"), this WP does NOT assert mirror-equivalence without a completed coverage-diff. Disposition: deferred-promotion (existing row = cli, not in the Phase-S capped set anyway) -- flagged as the single BEST candidate for a follow-up EG1 capture (run it standalone with a longer budget, outside WP01) before defaulting straight to a fresh measured-capture promotion. | n/a (EG1 capture attempted, aborted -- see evidence README) | A |
| `tests/specify_cli/cli/commands/review` | a | 1x - deferred-promotion | cli (existing row) | Leaf is itself PC1-clean (same as charter above). Owning row **cli** by import census (4x `specify_cli.cli.commands.review._diagnostics`, 3x `._dead_code`, 3x `specify_cli.cli.commands.review`). Census evidence (`grep -rl` in `tests/cli/`) found **ZERO** files in `tests/cli` referencing `commands.review` or `commands/review` -- unlike the charter sibling, there is no plausible in-matrix mirror at all, so no coverage-diff was attempted (would certainly show no overlap). Disposition: deferred-promotion (existing row = cli, not in the Phase-S capped set). | n/a (no plausible mirror found by census, no capture attempted) | A |
| `tests/specify_cli/lanes` | a | 1x - deferred-promotion | lanes (existing row) | Existing owning row **lanes** confirmed by import census (15x `specify_cli.lanes.worktree_allocator`, 7x `.models`, 7x `.recovery`). PC1 clean. NOT in the Phase-S capped set (merge/status/dashboard/auth only) -> promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Owning row: lanes. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/missions` | a | 1x - deferred-promotion | missions (existing row) | Existing owning row **missions** confirmed by import census (18x `specify_cli.missions._read_path_resolver`, 6x `specify_cli.missions._substantive`). PC1 clean. NOT in the Phase-S capped set (merge/status/dashboard/auth only) -> promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Owning row: missions. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/next` | a | 1x - deferred-promotion | next (existing row) | Existing owning row **next** confirmed by import census (39x `runtime.next.runtime_bridge`, 14x `runtime.next._internal_runtime.workflow_registry`). PC1 clean. NOT in the Phase-S capped set (merge/status/dashboard/auth only) -> promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Owning row: next. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/post_merge` | a | 1x - deferred-promotion | post_merge (existing row) | Existing owning row **post_merge** confirmed by import census (5x `specify_cli.post_merge.retrospective_terminus`). PC1 clean. NOT in the Phase-S capped set (merge/status/dashboard/auth only) -> promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Owning row: post_merge. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/review` | a | 1x - deferred-promotion | review (existing row) | Existing owning row **review** confirmed by import census (3x `specify_cli.review.cycle`, 2x `specify_cli.review`). PC1 clean. NOT in the Phase-S capped set (merge/status/dashboard/auth only) -> promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Owning row: review. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/upgrade` | a | 1x - deferred-promotion | upgrade (existing row) | Existing owning row **upgrade** confirmed by import census (20x `specify_cli.upgrade.registry`, 18x `.migrations`, 9x `.metadata`). PC1 clean. NOT in the Phase-S capped set (merge/status/dashboard/auth only) -> promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Owning row: upgrade. | n/a (deferred, no capture run) | A |
| `tests/specify_cli/upgrade/migrations` | a | 1x - deferred-promotion | upgrade (existing row) | Existing owning row **upgrade** confirmed by import census (20x `specify_cli.upgrade.registry`, 18x `.migrations`, 9x `.metadata`). PC1 clean. NOT in the Phase-S capped set (merge/status/dashboard/auth only) -> promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Owning row: upgrade. Nested under the (also deferred) `tests/specify_cli/upgrade` parent; PC1 is satisfied because neither is currently claimed by another row, so recursion will cover this dir automatically once the parent is promoted -- record BOTH as the same owner/decision, never promote parent+child separately (non-nesting guard). | n/a (deferred, no capture run) | A |
| `tests/specify_cli/docs` | a | 2 - lift-invariant | tests/architectural/ (3 lifts, see T006) | Unique value is single-topic invariant/regression-freeze checks, not broad behavioral coverage: `test_trail_model_doc.py`, `test_host_surface_inventory.py`, `test_readme_governance.py`. Disposition 2 (lift) per the #4715 precedent (`tests/architectural/test_completion_manifest_freshness.py`) -- WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py` gate; see T006. | n/a (lift needs no measurement) | A |
| `tests/specify_cli/drg_writers` | a | 2 - lift-invariant | tests/architectural/test_lifted_drg_writers_registry_completeness.py | Unique value is single-topic invariant/regression-freeze checks, not broad behavioral coverage: `test_registry_completeness.py`. Disposition 2 (lift) per the #4715 precedent (`tests/architectural/test_completion_manifest_freshness.py`) -- WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py` gate; see T006. | n/a (lift needs no measurement) | A |
| `tests/specify_cli/mission_v1` | a | 2 - lift-invariant | tests/architectural/test_lifted_mission_v1_import_hygiene.py | Unique value is single-topic invariant/regression-freeze checks, not broad behavioral coverage: `test_import_hygiene.py`. Disposition 2 (lift) per the #4715 precedent (`tests/architectural/test_completion_manifest_freshness.py`) -- WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py` gate; see T006. | n/a (lift needs no measurement) | A |
| `tests/integration` | b | 3 - sharpen-reason | special_tiers.integration_tests_next | Already claimed by `special_tiers.integration_tests_next` (roots `tests/integration/**`, `tests/next/**`; job `integration-tests-next`, ~69min measured). PC3: disposition must reconcile with this tier, not mint a fresh row. The job is currently **unwired** (not invoked by any triggering workflow) -- a pre-existing honesty gap, not something this WP silently papers over. Recorded as a decision: stays out-of-matrix, home is the special tier; file a follow-up to wire the job. | n/a (special-tier claim, no new evidence needed) | I |
| `tests/integration/core` | b | 3 - sharpen-reason | special_tiers.integration_tests_next | Already claimed by `special_tiers.integration_tests_next` (roots `tests/integration/**`, `tests/next/**`; job `integration-tests-next`, ~69min measured). PC3: disposition must reconcile with this tier, not mint a fresh row. The job is currently **unwired** (not invoked by any triggering workflow) -- a pre-existing honesty gap, not something this WP silently papers over. Recorded as a decision: stays out-of-matrix, home is the special tier; file a follow-up to wire the job. | n/a (special-tier claim, no new evidence needed) | I |
| `tests/integration/git` | b | 3 - sharpen-reason | special_tiers.integration_tests_next | Already claimed by `special_tiers.integration_tests_next` (roots `tests/integration/**`, `tests/next/**`; job `integration-tests-next`, ~69min measured). PC3: disposition must reconcile with this tier, not mint a fresh row. The job is currently **unwired** (not invoked by any triggering workflow) -- a pre-existing honesty gap, not something this WP silently papers over. Recorded as a decision: stays out-of-matrix, home is the special tier; file a follow-up to wire the job. | n/a (special-tier claim, no new evidence needed) | I |
| `tests/integration/migration` | b | 3 - sharpen-reason | special_tiers.integration_tests_next | Already claimed by `special_tiers.integration_tests_next` (roots `tests/integration/**`, `tests/next/**`; job `integration-tests-next`, ~69min measured). PC3: disposition must reconcile with this tier, not mint a fresh row. The job is currently **unwired** (not invoked by any triggering workflow) -- a pre-existing honesty gap, not something this WP silently papers over. Recorded as a decision: stays out-of-matrix, home is the special tier; file a follow-up to wire the job. | n/a (special-tier claim, no new evidence needed) | I |
| `tests/integration/retrospective` | b | 3 - sharpen-reason | special_tiers.integration_tests_next | Already claimed by `special_tiers.integration_tests_next` (roots `tests/integration/**`, `tests/next/**`; job `integration-tests-next`, ~69min measured). PC3: disposition must reconcile with this tier, not mint a fresh row. The job is currently **unwired** (not invoked by any triggering workflow) -- a pre-existing honesty gap, not something this WP silently papers over. Recorded as a decision: stays out-of-matrix, home is the special tier; file a follow-up to wire the job. | n/a (special-tier claim, no new evidence needed) | I |
| `tests/integration/review` | b | 3 - sharpen-reason | special_tiers.integration_tests_next | Already claimed by `special_tiers.integration_tests_next` (roots `tests/integration/**`, `tests/next/**`; job `integration-tests-next`, ~69min measured). PC3: disposition must reconcile with this tier, not mint a fresh row. The job is currently **unwired** (not invoked by any triggering workflow) -- a pre-existing honesty gap, not something this WP silently papers over. Recorded as a decision: stays out-of-matrix, home is the special tier; file a follow-up to wire the job. | n/a (special-tier claim, no new evidence needed) | I |
| `tests/integration/sparse_checkout` | b | 3 - sharpen-reason | special_tiers.integration_tests_next | Already claimed by `special_tiers.integration_tests_next` (roots `tests/integration/**`, `tests/next/**`; job `integration-tests-next`, ~69min measured). PC3: disposition must reconcile with this tier, not mint a fresh row. The job is currently **unwired** (not invoked by any triggering workflow) -- a pre-existing honesty gap, not something this WP silently papers over. Recorded as a decision: stays out-of-matrix, home is the special tier; file a follow-up to wire the job. | n/a (special-tier claim, no new evidence needed) | I |
| `tests/_factories` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: test fixture/factory helpers (3 files) -- no marker, no owning row. | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/_support/coverage_safety` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: coverage-safety support helpers (1 file). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/_support/git_template` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: git-template support helpers (1 file). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/acceptance` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: own acceptance-gate suite (6 files: scaffold/consolidation/verdict/first_run/provenance). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/adversarial` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: adversarial-squad related tests (1 file). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/ast_analysis` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: AST-analysis tests (1 file). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/calibration` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: calibration tests (1 file). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/characterization` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: characterization tests (1 file). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/cli_gate` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: CLI gate tests (7 files). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/compat` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: compat suite (2 files: diagnostic_safe_predicate, dry_run_parity) -- distinct from pop-(a) rootless `specify_cli.compat` package tests. | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/concurrency` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: concurrency tests (1 file). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/context` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: context tests (1 file: mission_resolver) -- distinct from pop-(a) rootless `tests/specify_cli/context`. | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/contract/spec_kitty_events_consumer` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: spec_kitty_events contract-consumer tests (1 file). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/contract/spec_kitty_tracker_consumer` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: spec_kitty_tracker contract-consumer tests (1 file). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/cross_branch` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: cross-branch tests (1 file). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/cross_cutting/packaging` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: cross-cutting packaging tests (3 files) -- sibling `cross_cutting/*` dirs above ARE marker-verified; this one is not. | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/cross_cutting/versioning` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: cross-cutting versioning tests (3 files) -- same caveat as packaging above. | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/dossier` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: own dossier suite (15 files: api/models/events/manifest/manifest_guard_parity); potential mirror-pair with pop-(a) rootless `tests/specify_cli/dossier` (1 file) -- neither in-matrix, no sharpen admissible. | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/git_ops` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: own git_ops suite (18 files: safe_commit/stale_detection/worktree/branch_contract/sync_staleness) -- substantial, real behavioral coverage, explicitly named in spec.md's population-(b) example list. | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/init` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: init tests (9 files). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/invocation` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: invocation suite (2 files: adapters, dispatch_recommendation) -- distinct from pop-(a) rootless `tests/specify_cli/invocation` (30 files). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/migrate` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: migrate tests (1 file) -- third similarly-named tree alongside pop-(a) `tests/specify_cli/migration` (rootless) and pop-(b) `tests/migration` (marker-verified sharpen); flagged for follow-up consolidation triage. | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/perf` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: perf tests (2 files) -- distinct from marker-verified `tests/performance`. | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/prompts` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: prompt tests (2 files). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/readiness` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: readiness tests (7 files) -- overlaps rootless pop-(a) `tests/specify_cli/readiness` (1 file, `specify_cli.compat.remediation`) and rootless `compat`. | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/research` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: research tests (4 files). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/scripts` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: scripts tests (2 files: sync_all_contributors, quality_gate_decision). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/specify_cli/acceptance` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.acceptance` package (17 files; matrix/gates_core submodules). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/audit` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.audit` package (shape_registry/detectors/engine/models). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/bootstrap` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: `specify_cli.bootstrap` + `specify_cli` init/env_file. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/bulk_edit` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.bulk_edit` package (occurrence_map/diff_check/gate). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/compat` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.compat` package (26 files; planner/cache/_detect/provider). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/config` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: `specify_cli.config.path_conventions`. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/context` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.context` package (models/store/errors) -- NOT covered by core_misc's `src/specify_cli/core/**` root despite importing `specify_cli.core.paths` as a dependency. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/contracts` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.contracts` package (anchoring/registry). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/decisions` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.decisions` package (models/emit/store). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/distribution` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.distribution` package (package_name/upgrade_provider/simple_index). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/doctrine` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.doctrine` package (sources/https_source/template_render) -- distinct from `src/charter/**`, NOT the charter row's root. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/dossier` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.dossier` package (snapshot/indexer/manifest/hasher); potential mirror-pair with pop-(b) `tests/dossier` (also rootless -- neither is in-matrix, so no sharpen is admissible, see EG1 note in README). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/events` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: mixed: `runtime.next.*` (8x, overlaps the `next` row's cov_target) + own unclaimed `specify_cli.events.decision_log`; not a clean subset so not folded into `next`. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/git` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.git` package (commit_helpers/ref_advance) + `kernel.*`; potential mirror-pair with pop-(b) `tests/git`/`tests/git_ops` (also rootless). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/identity` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.identity.project` (mission-id canonical identity, mission 083). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/integration` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: mixed: mostly `specify_cli.status.*` (overlaps the status row being promoted) + `specify_cli.migration.rebuild_state`; NOT a clean subset (mixed imports) so not bundled into the status promotion the way write_side was. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/invocation` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.invocation` package (30 files; executor/writer/errors/propagator/registry). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/invocation/cli` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: nested under `tests/specify_cli/invocation` (also deferred, same owner-status); PC1 satisfied, promote together. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/live_work` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.live_work` package (kinds/models) + `specify_cli.zeitgeist_client` (client of core_misc's claimed package, but live_work itself is unclaimed). **#4426 dir** -- this decision (deferred-promotion) closes #4426's obligation to decide it, per SC-008. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/migration` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.migration` package (25 files; backfill_runtime_state/rebuild_state); potential mirror-pair with pop-(b) `tests/migration` (marker-verified sharpen) and `tests/migrate` (also rootless) -- three similarly-named trees, none in-matrix except via this WP's own promotions; flagged for a follow-up consolidation triage. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/mission_step_contracts` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.mission_step_contracts.executor`; overlaps `charter.offering.missions.step_contracts` (missions row's cov_target) but its OWN src package `src/specify_cli/mission_step_contracts/**` is not in the missions row's `roots`. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/orchestrator_api` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.orchestrator_api` package (commands, 14x). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/ownership` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.ownership` package (models/validation/frontmatter_source) + `specify_cli.status.wp_metadata` (weak overlap only). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/readiness` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: `specify_cli.compat.remediation` (9x) -- overlaps the rootless `compat` package, not an owning row. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/regression` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: cross-cutting regression suite spanning `orchestrator_api`, `missions`, `events`, `next`, `tasks` -- no single owning row could claim it without an aggregate-fold (prohibited); potential mirror-pair with pop-(b) `tests/regressions` (marker-verified sharpen, different tree). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/retrospect` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: `specify_cli.retrospective.generator`; note the THREE similarly-named trees -- pop-(a) `retrospect` (1 file), pop-(a) `retrospective` (8 files), pop-(b) `retrospective` (22 files, marker-verified) -- flagged for a follow-up naming/consolidation triage, not resolved here. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/retrospective` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.retrospective` package (lifecycle_events/policy) + `specify_cli.status`, `specify_cli.workspace.root_resolver`; see retrospect/retrospective triple-name flag above. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/saas_client` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.saas_client` package (errors) + `specify_cli.auth` (2x, weak) + `specify_cli.zeitgeist_client` (weak). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/session_presence` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.session_presence` package (content/writers/upgrade_check). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/shims` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.shims` package (registry/generator) + `specify_cli.migration.rewrite_shims`. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/skills` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own, REAL, currently-UNCLAIMED `src/specify_cli/skills/**` package (mission 083 per CLAUDE.md: command_renderer.py, command_installer.py, manifest_store.py) -- 37x `specify_cli.skills`, 27x `specify_cli.tool_surface.operations` (tool_surface itself IS claimed by core_misc, but skills is not); genuinely unique behavioral coverage of a real, documented package. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/tasks` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.tasks` package (issue_matrix/issue_matrix_migration) + `specify_cli.coordination` (weak, status row's 2nd cov_target, not a clean subset). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/tasks_authoring` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.tasks_authoring` package (post_integration_warning). | n/a (deferred, no capture run) | B |
| `tests/specify_cli/test_cli` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: `specify_cli.frontmatter`, `specify_cli.coordination.commit_router` -- ambiguous/legacy-named single-file dir, no owning row. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/tracker` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: `specify_cli.tracker.config`/`.saas_service`; potential mirror-pair with pop-(b) `tests/tracker` (marker-verified sharpen, 27 files, much larger) -- flagged for follow-up consolidation. | n/a (deferred, no capture run) | B |
| `tests/specify_cli/widen` | a | 1x - deferred-promotion | none (rootless -- needs new row via scrub) | No `modules[]` row's `roots`/`cov_targets` names this package (checked against the full 20-row registry). Real, unique behavioral coverage (multi-file, non-invariant). Minting a row is a census-gated two-file edit (scrub + registry) -- heavier than an existing-row add and outside the Phase-S security-first cap. Promotion warranted; deferred to a follow-up to respect the bounded single-PR cap (#4374). Import census: own `specify_cli.widen.models` + `specify_cli.saas_client`/`specify_cli.decisions` (weak overlaps). | n/a (deferred, no capture run) | B |
| `tests/tasks` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: own tasks-finalize suite (19 files: finalize_sequential_overlap, finalize_planning_artifact_kitty_specs, finalize_wps_yaml_versioned, finalize_ownership_routing, tasks_2x_unit) -- distinct from pop-(a) rootless `tests/specify_cli/tasks` (5 files, `specify_cli.tasks.issue_matrix*`); substantial, real behavioral coverage, no owning row. | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/test_template` | b | 1x - deferred-promotion | none (rootless / marker-gap) | T004 marker-presence grep found NO `{corpus,windows_ci,performance,e2e,stress}` marker in this tree. The pre-mission reason implied marker-reachability without verification -- that would be a #2979-class disguised snapshot if kept as-is. Correct disposition: this tree needs either a real marker applied (and CI wiring) or a per-PR/nightly lane; deferred-promotion recorded as an honest decision (not a snapshot), routed to a follow-up. Content: template tests (3 files). | analysis/evidence/pop_b_marker_grep.txt (absence recorded) | B |
| `tests/evidence` | b | 2 - lift-invariant | tests/architectural/test_lifted_evidence.py | Unique value is single-topic invariant/regression-freeze checks, not broad behavioral coverage: `test_project_sync_consent_manifest.py`. Disposition 2 (lift) per the #4715 precedent (`tests/architectural/test_completion_manifest_freshness.py`) -- WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py` gate; see T006. | n/a (lift needs no measurement) | B |
| `tests/lint` | b | 2 - lift-invariant | tests/architectural/test_lifted_lint.py | Unique value is single-topic invariant/regression-freeze checks, not broad behavioral coverage: `test_canonical_producers.py`. Disposition 2 (lift) per the #4715 precedent (`tests/architectural/test_completion_manifest_freshness.py`) -- WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py` gate; see T006. | n/a (lift needs no measurement) | B |
| `tests/mission_metadata` | b | 2 - lift-invariant | tests/architectural/test_lifted_mission_metadata.py | Unique value is single-topic invariant/regression-freeze checks, not broad behavioral coverage: `test_mission_identity.py`. Disposition 2 (lift) per the #4715 precedent (`tests/architectural/test_completion_manifest_freshness.py`) -- WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py` gate; see T006. | n/a (lift needs no measurement) | B |
| `tests/packaging` | b | 2 - lift-invariant | tests/architectural/test_lifted_packaging_windows_keyring.py | Unique value is single-topic invariant/regression-freeze checks, not broad behavioral coverage: `test_windows_no_keyring.py`. Disposition 2 (lift) per the #4715 precedent (`tests/architectural/test_completion_manifest_freshness.py`) -- WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py` gate; see T006. **#2979 note**: the filename strongly implies Windows-specific behavior, but T004's grep found NO `windows_ci` marker on this tree -- a real, concrete #2979-class gap (the test *should* plausibly run in ci-windows.yml but currently reaches CI only via `make test-full`). Lifting it into `tests/architectural` (always-on, platform-agnostic assertion about the keyring-absence code path) sidesteps the gap rather than requiring a Windows runner; flagged for WP02 to confirm the assertion is genuinely platform-independent before lifting. | n/a (lift needs no measurement) | B |
| `tests/proof` | b | 2 - lift-invariant | tests/architectural/test_lifted_proof.py | Unique value is single-topic invariant/regression-freeze checks, not broad behavioral coverage: `test_event_schemas.py`. Disposition 2 (lift) per the #4715 precedent (`tests/architectural/test_completion_manifest_freshness.py`) -- WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py` gate; see T006. | n/a (lift needs no measurement) | B |
| `tests/reviews` | b | 2 - lift-invariant | tests/architectural/test_lifted_reviews.py | Unique value is single-topic invariant/regression-freeze checks, not broad behavioral coverage: `test_review_gate_activation.py`. Disposition 2 (lift) per the #4715 precedent (`tests/architectural/test_completion_manifest_freshness.py`) -- WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py` gate; see T006. | n/a (lift needs no measurement) | B |
| `tests/specify_cli` | b | 2 - lift-invariant | tests/architectural/ (representative sample, see T006) | PC1 PROMOTION-BLOCKED: descendants include the excluded `tests/specify_cli/cli/commands` AND multiple claimed dirs (`tests/specify_cli/core`, `.../tool_surface`, `.../coordination`, `.../cli/commands/agent`, `.../runtime`). Its ~70 DIRECT files (not counting the pop-(a) subtrees, each separately dispositioned above) are overwhelmingly single-topic regression/guard/contract/migration checks by naming convention (`test_*_regression`, `test_*_guard`, `test_*_contract`, `test_*_migration`) spanning many unrelated src modules (mission metadata, charter lifecycle, acceptance gate, meta read/write fail-closed paths, doctor, workspace, worktrees index, gitignore, atomic write, ...). Disposition 2 (lift), but the set is TOO LARGE (~70 files) to enumerate exhaustively in this WP -- T006 names a representative sample for WP02 and flags that WP02 must triage/batch the remainder by family rather than lift all ~70 in one pass. | n/a (lift needs no measurement) | B |
| `tests/state` | b | 2 - lift-invariant | tests/architectural/test_lifted_state.py | Unique value is single-topic invariant/regression-freeze checks, not broad behavioral coverage: `test_doctor_spec_kitty_home.py`. Disposition 2 (lift) per the #4715 precedent (`tests/architectural/test_completion_manifest_freshness.py`) -- WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py` gate; see T006. | n/a (lift needs no measurement) | B |
| `tests/task_utils` | b | 2 - lift-invariant | tests/architectural/test_lifted_task_utils.py | Unique value is single-topic invariant/regression-freeze checks, not broad behavioral coverage: `test_set_scalar_retired.py`. Disposition 2 (lift) per the #4715 precedent (`tests/architectural/test_completion_manifest_freshness.py`) -- WP02 lifts each into an always-on `tests/architectural/test_lifted_<x>.py` gate; see T006. | n/a (lift needs no measurement) | B |
| `tests/_support` | b | 3 - sharpen-reason | nightly stress lane | Marker-presence grep VERIFIED (T004): `pytest.mark.stress` found in this tree. Sharpened reason names the real marker home: stress. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/audit` | b | 3 - sharpen-reason | nightly performance lane | Marker-presence grep VERIFIED (T004): `pytest.mark.performance` found in this tree. Sharpened reason names the real marker home: performance. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/contract` | b | 3 - sharpen-reason | nightly corpus lane | Marker-presence grep VERIFIED (T004): `pytest.mark.corpus` found in this tree. Sharpened reason names the real marker home: corpus. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/cross_cutting` | b | 3 - sharpen-reason | nightly performance/stress lane | Marker-presence grep VERIFIED (T004): `pytest.mark.performance` (+ stress) found in this tree. Sharpened reason names the real marker home: performance/stress. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/cross_cutting/dashboard` | b | 3 - sharpen-reason | nightly performance/stress lane | Marker-presence grep VERIFIED (T004): `pytest.mark.performance` (+ stress) found in this tree. Sharpened reason names the real marker home: performance/stress. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/cross_cutting/encoding` | b | 3 - sharpen-reason | nightly performance lane | Marker-presence grep VERIFIED (T004): `pytest.mark.performance` found in this tree. Sharpened reason names the real marker home: performance. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/cross_cutting/misc` | b | 3 - sharpen-reason | nightly performance lane | Marker-presence grep VERIFIED (T004): `pytest.mark.performance` found in this tree. Sharpened reason names the real marker home: performance. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/doctor` | b | 3 - sharpen-reason | nightly performance lane | Marker-presence grep VERIFIED (T004): `pytest.mark.performance` found in this tree. Sharpened reason names the real marker home: performance. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/git` | b | 3 - sharpen-reason | nightly performance lane | Marker-presence grep VERIFIED (T004): `pytest.mark.performance` found in this tree. Sharpened reason names the real marker home: performance. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/migration` | b | 3 - sharpen-reason | nightly performance lane | Marker-presence grep VERIFIED (T004): `pytest.mark.performance` found in this tree. Sharpened reason names the real marker home: performance. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/mission` | b | 3 - sharpen-reason | nightly windows_ci lane | Marker-presence grep VERIFIED (T004): `pytest.mark.windows_ci` found in this tree. Sharpened reason names the real marker home: windows_ci. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/paths` | b | 3 - sharpen-reason | nightly windows_ci lane | Marker-presence grep VERIFIED (T004): `pytest.mark.windows_ci` found in this tree. Sharpened reason names the real marker home: windows_ci. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/performance` | b | 3 - sharpen-reason | nightly performance lane | Marker-presence grep VERIFIED (T004): `pytest.mark.performance` found in this tree. Sharpened reason names the real marker home: performance. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/policy` | b | 3 - sharpen-reason | nightly windows_ci lane | Marker-presence grep VERIFIED (T004): `pytest.mark.windows_ci` found in this tree. Sharpened reason names the real marker home: windows_ci. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/regressions` | b | 3 - sharpen-reason | nightly windows_ci/performance lane | Marker-presence grep VERIFIED (T004): `pytest.mark.windows_ci` (+ performance) found in this tree. Sharpened reason names the real marker home: windows_ci/performance. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/retrospective` | b | 3 - sharpen-reason | nightly performance lane | Marker-presence grep VERIFIED (T004): `pytest.mark.performance` found in this tree. Sharpened reason names the real marker home: performance. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/stress` | b | 3 - sharpen-reason | nightly stress lane | Marker-presence grep VERIFIED (T004): `pytest.mark.stress` found in this tree. Sharpened reason names the real marker home: stress. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
| `tests/tracker` | b | 3 - sharpen-reason | nightly windows_ci lane | Marker-presence grep VERIFIED (T004): `pytest.mark.windows_ci` found in this tree. Sharpened reason names the real marker home: windows_ci. Not a disguised snapshot (#2979 class) -- grep result recorded in analysis/evidence/pop_b_marker_grep.txt. | analysis/evidence/pop_b_marker_grep.txt | B |
